hacked() {
  sleep 5s;
  cd ~/Documents;
  mkdir "ive been hacked";
  cd ~/Documents/"ive been hacked";
  for i in `seq 1 100`
  do
    echo "ffs I got hacked again!" > "hacked$i.txt";
    code "hacked$i.txt";
    sleep 1s;
  done
}